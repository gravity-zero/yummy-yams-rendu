
export const registerUser = (userData) => async (dispatch) => {
    try {
      // Faites un appel fetch pour s'inscrire
      // Dispatchez une action appropriée si l'inscription réussit
    } catch (error) {
      // Dispatchez une action pour gérer les erreurs
    }
  };
  
  export const loginUser = (userData) => async (dispatch) => {
    try {
      // Faites un appel fetch pour se connecter
      // Dispatchez une action appropriée si la connexion réussit
    } catch (error) {
      // Dispatchez une action pour gérer les erreurs
    }
  };
  